# 修正内容の確認

## 1. 共有導線

- データ管理に `共有リンクを作成` ボタンを追加。
- ホーム画面から共有シートを開けるようにした。

## 2. 共有シート

- `ShareSheet` を新規作成。
- 収録機能:
  - リンクコピー
  - 他アプリ共有（対応端末）
  - JSON保存
- URL長表示と警告:
  - 推奨: 1800文字以下
  - 警戒: 4000文字超

## 3. 共有処理

- `HomeView` で共有URL生成処理を実装。
- 生成失敗/コピー失敗/共有失敗時のトースト通知を追加。

## 4. URL自動ロード

- `App` に `#s=<payload>` の自動復元処理を追加。
- 復元成功時にホーム画面へ戻し、ハッシュを除去。
- 復元失敗時はトーストで通知。

## 5. 検証結果

- `yarn lint` 成功
- `yarn build` 成功
- `yarn test:share-codec` 成功
